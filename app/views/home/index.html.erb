<% if user_signed_in? %>
<%= 'Hello user' %>
<h2><%= link_to 'Log out', destroy_user_session_path %>
</h2>
<% else %>
<%= render 'home/template'%>
<%# here I am going to apply a template to allow the user move through the app instead of the splash page, probably the categories page %>
<%end%>

